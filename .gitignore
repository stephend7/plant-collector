# macOS clutter
.DS_Store

# PRIVATE DATA — never publish these
*.xlsx
Photos - Just purchased plants want to enter them into the database/
docs/ModelPairedDev.pdf

# Local editor/agent tooling
.claude/

# Local-only folder for secrets/screenshots Claude may read but never publish
Claude to ignore/

# Dev-side test tooling (Phase B) — never part of the deployed app
node_modules/
.env
test-results/
playwright-report/
